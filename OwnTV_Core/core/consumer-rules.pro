# Keep rules that :core imposes on anything consuming it. Empty for now: the code arrives in
# Phase 5, and the rules that protect it still live in app/proguard-rules.pro until then. The
# file is declared by consumerProguardFiles so that moving a rule here later is a one-line change
# rather than a build-config change.
